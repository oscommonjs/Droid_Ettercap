/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-littlearm)
GROUP ( /data/data/hack.wireshark.android/libc/lib/libpthread.so.0 /data/data/hack.wireshark.android/libc/lib/libpthread_nonshared.a )
